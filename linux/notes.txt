9/24:
  - Fixed Dolphin not showing metadata
    - Purging the index might have done it?
      - balooctl6 purge    *** THIS SEEMS KEY ***
      - systemctl --user restart kde-baloo (not sure if this was needed)
      - Not sure if baloo was running before, maybe it needed to be turned on too
      - Don't think mediainfo was needed here after all
    - Anyway, this was one of the things that was really bugging me about my Debian install
      - so I'm glad it's working now! Less pressure to reboot
      
9/23:
  - Debian 13 Dolphin doesn't show video metadeta (dimensions, duration)
    - File Indexing is enabled
    - FFMPEGThumbs package was already installed
    - Values show up in file details, just not columns
    - Is this a bug in the debian version of Dolphin?
    - Not the biggest of deals, but annoying if it's just bugged in Debian
  - MacBook Air keeps sleeping but not hibernating
    - Turns out /etc/systemd/sleep.conf has default HibernateDelaySec of 2 hours.
      - Laptop is probably dying before then
      - Can try setting this shorter
      - Can test directly with sudo systemctl suspend-then-hibernate
      - Check logs with sudo journalctl -b -1 | grep -Ei 'hibernat'

9/22:
  - Should probably keep notes on this stuff like I have been with coding
  - Trying alternative editors
  - Kate
    - Comment selection: ctrl + c
    - Theme: has VSCodium dark, which is decent, but doesn't have quite my github dimmed
      - How hard would it be to make one?
    - UI: GUI, easy to style fonts/colors separately from terminal
    - Move lines up/down: Ctrl+Shift+Up/Down, can I switch to Alt+Up/Down?
    - Indent current line: ???
    - VSCode bindings
      - https://wrhol.com/blog/posts/kate-shortcuts/kate_vscode.shortcuts
      - https://github.com/mer30hamid/kate-vscode/blob/main/kate-vscode.shortcuts
